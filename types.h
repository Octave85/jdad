#ifndef TYPES_H
#define TYPES_H

#include <string.h>
#include "symtab.h"
#include "global.h"

typedef enum { String, Doble, Truthval, Object, Array } type_t;

typedef enum { False True Null } truthval_t;

/* TYPES_H */
#endif